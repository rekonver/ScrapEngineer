/// <summary>
/// Тип блоку у фізичній системі.
/// </summary>
public enum BlockType
{
    Undefined = 0,  // Значення за замовчуванням — корисне для перевірок :contentReference[oaicite:1]{index=1}
    Bearing = 1,    // Підшипник
    Damper = 2,     // Амортизатор
}
